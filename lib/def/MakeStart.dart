import 'package:flutter/material.dart';

class Makestart extends StatelessWidget {
  const Makestart({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
